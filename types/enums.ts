export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
  OTHER = 'OTHER',
}

export enum RoleType {
  ADMIN = 'ROLE_USER',
  USER = 'ROLE_ADMIN',
}

export enum EventStatus {
  FINISHED = 'FINISHED',
  PENDING = 'PENDING',
  ON_PROGRESS = 'ON_PROGRESS',
  CANCELLED = 'CANCELLED',
}
