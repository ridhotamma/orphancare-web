export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
  OTHER = 'OTHER',
}

export enum RoleType {
  ADMIN = 'ROLE_ADMIN',
  USER = 'ROLE_USER',
}

export enum EventStatus {
  FINISHED = 'FINISHED',
  PENDING = 'PENDING',
  ON_PROGRESS = 'ON_PROGRESS',
  CANCELLED = 'CANCELLED',
}
